import { ISubscriptionInfo } from "../../interfaces";
import { CloudConnectData, IdentityObserver } from "../../types";
export declare class CloudSubscriptionClient {
    private mqttClient;
    private observersSet;
    private subscriptionQueue;
    connect(data: CloudConnectData): Promise<void>;
    subscribe(subscription: ISubscriptionInfo, observer: IdentityObserver): Promise<void>;
    unsubscribe(subscription: ISubscriptionInfo, observerId: string): Promise<any>;
    private onReceive;
    private onClose;
    private subscribeInternal;
    private addObserver;
    private processResponse;
}
