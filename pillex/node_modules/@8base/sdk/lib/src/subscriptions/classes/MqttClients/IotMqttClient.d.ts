import { IMqttClient } from '../../interfaces';
import { IClientSubscribeOptions } from 'mqtt';
import { CloudConnectData } from "../../types";
export declare class IotMqttClient implements IMqttClient {
    private onReceive;
    private onClose;
    private client;
    connect(options: CloudConnectData, onReceive: Function, onClose: Function): Promise<IotMqttClient>;
    subscribe(topic: string, options: IClientSubscribeOptions): Promise<any>;
    unsubscribe(topic: string): Promise<void>;
    disconnect(): Promise<void>;
    private setCallback;
}
