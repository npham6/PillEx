import { NextLink, Operation } from "apollo-link";
import { SubscriptionConnectData } from "../../types";
export declare class ServerSubscriptionClient {
    private connectData;
    subscribe(operation: Operation, forwardedLink: NextLink): Promise<{
        action: string;
    }>;
    static unsubscribe(operation: Operation, forwardedLink: NextLink): Promise<void>;
    getConnectData(operation: Operation, forwardedLink: NextLink): Promise<SubscriptionConnectData>;
}
