import { IClientSubscribeOptions } from 'mqtt';
export interface IMqttClient {
    subscribe(topic: string, options: IClientSubscribeOptions): Promise<any>;
    unsubscribe(topic: string): Promise<void>;
    disconnect(): Promise<void>;
}
