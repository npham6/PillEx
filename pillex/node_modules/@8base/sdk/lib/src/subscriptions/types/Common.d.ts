import { ISubscriptionInfo } from "../interfaces/ISubscriptionInfo";
export interface ConnectCredentials {
    accessKeyId: string;
    secretKey: string;
    refreshToken: string;
}
export interface MqttConnectOptions extends ConnectCredentials {
    iotEndpoint: string;
    region: string;
    debug: boolean;
}
export interface SubscriptionConnectData extends MqttConnectOptions {
    user: string;
    workspace: string;
}
export interface CloudConnectData extends MqttConnectOptions {
    cloudUserName: string;
}
export declare function makeCloudConnectData(subscription: ISubscriptionInfo, mqttConnectOptions: MqttConnectOptions): CloudConnectData;
export interface IdentityObserver {
    observer: {
        next: Function;
        error: Function;
        complete: Function;
    };
    id: string;
}
