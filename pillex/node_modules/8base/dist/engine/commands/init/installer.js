"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
const path = require("path");
const _ = require("lodash");
const fs = require("fs");
const utils_1 = require("../../../common/utils");
function install(targetDirectory, files, context) {
    const repositoryName = path.basename(targetDirectory);
    context.logger.debug(`Start initialize repository with name "${repositoryName}" into path "${targetDirectory}"`);
    utils_1.Utils.safeExecution(_.bind(fs.mkdirSync, fs, targetDirectory));
    return utils_1.Utils.installFiles(targetDirectory, files, fs, context);
}
exports.install = install;
//# sourceMappingURL=installer.js.map